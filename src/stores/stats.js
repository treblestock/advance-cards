import { defineStore } from "pinia"

export const useStoreStats = defineStore('storeStats', {
  state: () => ({
    global: {},
    today: {},
    prevTask: {},
  }),
  getters: {
    
  },
  actions: {
    
  },
})